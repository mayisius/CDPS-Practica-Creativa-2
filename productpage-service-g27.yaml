apiVersion: v1
kind: Service
metadata:
  name: productpage-service
  namespace: cdps-27
spec:
  selector:
    app: productpage
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9095
  type: ClusterIP
